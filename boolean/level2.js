let age = 18;
let hasTicket = true;
let score = 45;

// Q1: Check if 'age' is greater than 15 AND 'hasTicket' is true.
// Hint: Use the && operator.
const ticket = age > 15 && hasTicket === true
console.log(ticket)


// Q2: Check if 'score' is exactly 50 OR 'score' is exactly 100.
// Hint: Use the || operator.
const check = score === 50 || score === 100
console.log(check)


// Q3: Check if 'age' is NOT equal to 20.
// Hint: Use the != or !== operator.
const equal = age !== 20
console.log(equal)